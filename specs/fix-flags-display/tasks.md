# fix-flags-display — Tasks

## Status

completed

## Tasks

- [x] 1. Remove individual player entries from `lib/utils/flags.ts`.
- [x] 2. Fix entry "Cape Verde" → "Cape Verde Islands" in `lib/utils/flags.ts`.
- [x] 3. Add "Jordan" → 🇯🇴 and "Congo DR" → 🇨🇩 to `lib/utils/flags.ts`.
- [x] 4. Verify that `pnpm tsc --noEmit` and `pnpm test` pass.
- [x] 5. Commit.

## Definition of Done

- [x] No player in Golden Boot shows a flag.
- [x] Cape Verde Islands, Jordan, and Congo DR show the correct flag.
- [x] `pnpm tsc --noEmit` passes.
- [x] `pnpm test` passes.
- [x] `specs/README.md` updated.
